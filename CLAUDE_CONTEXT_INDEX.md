# Claude context index

Start with `CLAUDE.md`, then `FILE_MAP.md`.

For full product context, read one compact file: `docs/CLAUDE_PROJECT_SUMMARY.md`.

For a coding task, read only the target HTML page and the corresponding file under `assets/js/pages/`. Read shared files only when required. This avoids spending context on unrelated screens.
